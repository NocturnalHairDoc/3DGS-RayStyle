# Project results dashboard

Open `project-results-dashboard.html` in a browser to inspect the complete
ten-phase result history. All referenced images, videos, and report files are
stored in `assets/`, so the folder is portable and does not depend on the local
`outputs/` directory.

The images are web-optimized copies of the original experiment renders. The
original full-resolution outputs remain excluded from Git.
